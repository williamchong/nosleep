export default defineAppConfig({
  ui: {
    colors: {
      // Override only the brand aliases; success (green) and error (red) keep Nuxt UI defaults.
      primary: 'blue',
      neutral: 'gray',
    },
  },
})
